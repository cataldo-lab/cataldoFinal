/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  // En Tailwind v4, la configuración se hace principalmente en CSS con @theme
  // Este archivo se mantiene para compatibilidad y configuraciones específicas de build
}