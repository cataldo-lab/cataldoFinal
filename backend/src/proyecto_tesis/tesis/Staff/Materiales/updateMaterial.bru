meta {
  name: updateMaterial
  type: http
  seq: 6
}

patch {
  url: http://localhost:3000/api/materiales/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{empleado_token}}
}

body:json {
  {
    "stock": 75,
    "precio_unitario": 2500
  }
}

settings {
  encodeUrl: true
}
