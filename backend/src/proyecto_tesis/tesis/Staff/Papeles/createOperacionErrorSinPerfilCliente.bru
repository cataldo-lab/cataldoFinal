meta {
  name: createOperacionErrorSinPerfilCliente
  type: http
  seq: 13
}

post {
  url: http://localhost:3000/api/papeles/operacion
  body: json
  auth: bearer
}

auth:bearer {
  token: {{empleado_token}}
}

body:json {
  {
    "operacion": {
      "id_cliente": 10,
      "estado_operacion": "pendiente",
      "descripcion_operacion": "Test error - usuario sin perfil de cliente"
    },
    "productos": [
      {
        "id_producto": 1,
        "cantidad": 1
      }
    ]
  }
}

settings {
  encodeUrl: true
}
