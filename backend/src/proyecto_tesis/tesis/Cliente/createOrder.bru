meta {
  name: createOrder
  type: http
  seq: 4
}

post {
  url: http://localhost:3000/api/cliente/my-orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{cliente_token}}
}

body:json {
  {
    "producto_id": 1,
    "cantidad": 2,
    "notas": "Entrega urgente"
  }
}

settings {
  encodeUrl: true
}
